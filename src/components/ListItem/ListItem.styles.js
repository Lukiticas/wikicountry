import styled from "styled-components";

const ListTitle = styled.p`
  color: inherit;
  font-size: 1rem;
  font-weight: 600;
`;

const ListSubtitle = styled.span`
  color: inherit;
  font-size: 1rem;
  font-weight: 300;
  margin-left: 0.3ch;
`;

export { ListSubtitle, ListTitle };
